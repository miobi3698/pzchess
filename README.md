# PzChess

A chess puzzle trainer.

## Build Instructions

### Dependencies

- Node.js (v18+)
- pnpm
- SQLite (via `sqlite` tool)

### Commands

Install dependencies and start the dev server:

```bash
pnpm install
pnpm dev
```
